package com.ice.core.interfaces {

		/**
		* Since AS3 doesn't support multiple inheritance, this interface is used to unify elements that move ( characters, bullets, etc )
		* under a single class definition. 
		*/
		public interface MovingObject {


		}

}
